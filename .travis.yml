language: node_js
node_js: "node"
cache:
  directories:
    - "node_modules"

services:
  - mysql

os:
  - windows

install:
  - mysql -e 'CREATE DATABASE IF NOT EXISTS graphql_server_boilerplate;'
  - npm install
  - npm start
